import test from 'tape'

import {
  TermVariable, TypeEnv, Set, List
} from '../lib/core'

import {
  ValueExpression,
  VariableExpression,
  BodyExpression,
  TypedVariableExpression
} from '../lib/expr'

import {
  LiteralType
} from '../lib/type'

test('type expression test', assert => {
  assert.test('type variable', assert => {

  })
})
