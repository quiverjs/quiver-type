import { sumType } from './sum'
import { productType } from './product'
import { isNode, assertRecord } from '../../container'

export const adtFromRecord = adtRecord => {
  assertRecord(adtRecord)
}

export const adtFromObject = adtObject => {

}
